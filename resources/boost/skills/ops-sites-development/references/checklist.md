# Ops Sites Checklist

- confirm features, ops modules, install steps, and doctor checks still match the real package surface
- confirm create, edit, and archive flows remain centralized in `MutateSiteAction`
- confirm pages still degrade safely when the store is unavailable
- confirm package tests cover changed mutation, resolver, or page flows
